
public class queue {

	 int delx; 
	   int dely;
	   int id; //SenderID
	   int uID; //OperationID
	   double clkcount;
	
	   public queue(int dx, int dy, int sid, int uid, double clkcount2)
			   {
				   delx = dx;
				   dely = dy;
				   id = sid;
				   uID = uid;
				   clkcount = clkcount2; 
			   }
		}
